name: "ITMarcosCarranza Action in Action"
inputs:
  who-to-greet: #id of input
    description: 'who to greet'
    required: true
    default: 'world'
output:
  answer: #id of output
    description: 'The answer to everything (always 42)'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.who-to-greet }}
